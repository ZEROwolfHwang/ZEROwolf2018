<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:layout_width="match_parent"
              android:layout_height="match_parent">
    <LinearLayout
        android:id="@+id/layout_bottom_sure"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:gravity="center"
        android:padding="@dimen/layout_margin10dp"
        android:elevation="3dp">


        <ImageView
            android:id="@+id/iv_car_goods_sure"
            android:layout_width="@dimen/layout_margin80dp"
            android:scaleType="fitCenter"
            android:background="@drawable/car_goods_dark"
            android:layout_marginLeft="@dimen/layout_margin10dp"
            android:layout_height="@dimen/layout_margin80dp"/>

        <include
            android:id="@+id/layout_group_sure"
            layout="@layout/layout_text_group_sure"/>

    </LinearLayout>

    <android.support.v7.widget.RecyclerView
        android:padding="@dimen/layout_margin5dp"
        android:id="@+id/recycler_view_goods_sure"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:focusable="false"/>

    <Button
        android:id="@+id/bt_ok_msg"
        android:text="@string/ok_send"
        style="@style/style_button_sure"/>

</LinearLayout>